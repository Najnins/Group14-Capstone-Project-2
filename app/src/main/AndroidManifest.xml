<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.arad.care4pets">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Care4Pets"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Care4Pets">
        <activity
            android:name=".ui.activities.HealthRecordDetails"
            android:exported="false" />
        <activity android:name=".ui.activities.DashboardActivity" />
        <activity android:name=".ui.activities.HealthRecordsActivity" />
        <activity android:name=".ui.activities.AddHealthRecordActivity" />
        <activity android:name=".ui.activities.CareInstructionsActivity" />
        <activity android:name=".ui.activities.AddReminderActivity" />
        <activity android:name=".ui.activities.EditReminderActivity" />
        <activity android:name=".ui.activities.RemindersActivity" />
        <activity android:name=".ui.activities.PetProfileActivity" />
        <activity android:name=".ui.activities.AddPetActivity" />
        <activity android:name=".ui.activities.EditPetActivity"/>
        <activity android:name=".ui.activities.PetsListActivity" />
        <activity android:name=".ui.activities.MenuActivity" />
        <activity android:name=".ui.activities.LoginActivity" />
        <activity android:name=".ui.activities.OnboardingActivity" />
        <activity android:name=".ui.activities.SignupActivity" /> <!-- Launcher (Splash) -->
        <activity
            android:name=".ui.activities.SplashActivity"
            android:exported="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".notification.ReminderNotificationReceiver"
            android:exported="false"/>

        <receiver
            android:name=".notification.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>